@import './quasar.variables'
@import '~quasar-styl'
// @import '~quasar-addon-styl'

.router-link-active
  color $info

.q-input-target
  color $standard-text-color

.q-menu
  color $standard-text-color
  background-color $block-background

.text-dark-bright {
  color: $white_very;
}

.bg-dark-bright {
  background: $dark_bright;
}

.q-stepper-tab.step-waiting, .q-stepper-step-content {
  color: $white_very;
}

.q-stepper, .q-stepper-header {
  box-shadow: none;
}

.q-stepper-horizontal .q-stepper-step-inner,
.q-stepper-tab {
  padding: 24px 0;
}

.q-item-sublabel {
  color: $white;
}

.dashboard-item .q-card {
  box-shadow: none;
}

.toolbar-buttons .q-btn {
  margin-left: 10px;
}

.q-list-container {
  padding: 16px;
}

.q-item-label {
  display: flex;
  align-items: center;
}

.q-item-label .q-btn {
  margin-left: auto;
  margin-right: 8px;
}

.q-list .editing .q-item {
  cursor: move;
}

.q-card-title {
  max-width: 100%;
}

.widget-container {
  height: 100%;
  width: 100%;
  display: flex;
  flex-direction: column;
}

.widget-modal {
  width: 800px;
  max-width: 100vw;
  padding: 10px;
  display: flex;
}

.widget-body {
  flex: 1 0;
  overflow: auto;
  flex-wrap: nowrap;
  justify-content: space-around;
  align-content: center;
  flex-direction: column;
}

.centered {
  margin: auto;
}

.editable {
  border-bottom: 1px solid gray;
  cursor: pointer;
  margin-bottom: 5px;
}

.darkened {
  color: grey;
}

.unpadded {
  padding: 0px;
}

.title-bar {
  padding: 5px 10px;
  width: 100%;
}

.q-card-main {
  padding: 10px;
}

.build-info {
  bottom: 0;
  position: absolute;
}

.inline-popup {
  display: inline-block;
  margin: 0px 10px;
}
